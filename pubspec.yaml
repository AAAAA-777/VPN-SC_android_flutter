name: vpn_sc
description: VPN-SC Android VLESS client
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_v2ray_plus:
    path: packages/flutter_v2ray_plus
  vpn_permission: ^0.0.2
  http: ^1.2.2
  shared_preferences: ^2.3.3
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
